server:
  port: 8080

spring:
  application:
    name: icampus-api
  datasource:
    url: jdbc:mysql://localhost:3306/icampus?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USERNAME:root}
    password: Ltc20061106

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:/mapper/**/*.xml

# 大模型配置
llm:
  api-key: sk-30350ee9ba8748a1a4091cf3bae6801d
  model: deepseek-chat

# JWT 配置
jwt:
  secret: icampus-jwt-secret-key-2024-min-length-32
  expiration-ms: 86400000  # 24小时

# 日志
logging:
  level:
    com.icampus: DEBUG
    org.springframework.security: INFO
