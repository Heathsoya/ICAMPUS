server:
  port: 8080

spring:
  application:
    name: icampus-api

# JWT 配置
jwt:
  secret: icampus-jwt-secret-key-2024-min-length-32
  expiration-ms: 86400000  # 24小时

# 日志
logging:
  level:
    com.icampus: DEBUG
    org.springframework.security: INFO
