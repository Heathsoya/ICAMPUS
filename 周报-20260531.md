# 周报 — 2026.05.27 ~ 2026.05.31

## 一、本周综述

本周完成了 ICAMPUS 校园智能问答系统 **api 模块** 的完整搭建，具体包括：

### 1. 项目架构搭建
- 建立 Maven 多模块结构：`core → domain → infra → app → api`
- api 依赖 app，app 依赖 domain+infra，domain 纯 POJO 零框架依赖
- 安装 Maven Wrapper，解决跨环境编译问题

### 2. REST Controller 实现（8 个接口）
| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录，返回 JWT Token |
| POST | `/api/qna/ask` | 智能问答（LLM 分析 → 知识库检索 → LLM 生成答案 → 记录日志） |
| GET | `/api/qna/hot` | 热点问题榜单 |
| POST | `/api/qna/feedback` | 答案反馈（支持防重复提交） |
| POST | `/api/contribution` | 用户知识贡献（含敏感词检测） |
| GET | `/api/admin/audit` | 管理员审核列表 |
| POST | `/api/admin/audit` | 管理员执行审核 |

### 3. 基础设施
- **统一响应包装**：`ApiResponse<T>`（code + message + data）
- **参数校验**：`@Valid` + Jakarta Validation 注解
- **JWT 认证集成**：`JwtAuthenticationFilter` + `SecurityConfig`，公私接口分离
- **全局异常处理**：`BizException` + `BizCode` 枚举（10 个错误码），自动转 JSON
- **LLM 客户端**：`MockLlmClient`（Mock 实现）+ `LlmClient` SPI 接口
- **内存 Repository**：5 个 InMemory 实现用于开发阶段

### 4. Git 协作
- 建立 `feat/api-module` 功能分支
- 配置 SSH over 443 解决校园网 GitHub 连接问题
- 通过 PR 合并到 main 分支

### 5. 测试验证
- 启动 Spring Boot 服务，8 个接口全部测试通过
- 中文提问正确匹配知识库，confidence=0.95

---

## 二、本周建议和意见

1. **app 模块命名问题**：当前 `app` 模块与前端 `webapp` 容易混淆。建议将 `app` 改名为 `service` 或 `business`，语义更清晰。

2. **数据库尽快落地**：目前使用内存 Repository，重启数据丢失。建议 infra 组尽快完成 MySQL 数据源配置和 MyBatis-Plus Mapper 实现。

3. **LLM 接入**：当前 `MockLlmClient` 只是关键词匹配。建议 infra 组优先对接大模型 API（通义千问/文心一言），实现真正的智能问答。

4. **前端联调**：后端 8 个接口已就绪，建议前端组尽快开始对接，提前发现接口字段问题。

5. **CI/CD**：建议配置 GitHub Actions 自动编译，每次 PR 自动跑 `mvn test`，避免合并后才发现编译错误。

---

## 三、下周计划

1. **LLM 真实接入**：用 WebClient 对接通义千问 API，替换 MockLlmClient
2. **数据库层落地**：配置 MySQL 数据源，实现 MyBatis-Plus Mapper 替换内存存储
3. **单元测试**：为 Service 层和 Controller 层编写 JUnit 测试
4. **接口文档**：生成 Swagger/OpenAPI 文档，方便前端对接
5. **前端联调**：配合前端组完成登录、问答、反馈流程的端到端调试
6. **代码 Review**：团队交叉 Review，确保代码规范统一
